name: Build ChatGPTEnhancer
# alpha50 restores sidebar/non-current conversation Rename + Markdown export without borrowing the active conversation id
# current top-right Pull/Reload/Rename/Export keeps alpha49 immutable exact-current targeting
# duplicate sidebar titles remain explicit candidate selection; arbitrary menu/config UUIDs are never sidebar identity
# Reload completion, project-header presentation and interrupted-generation recovery behavior are otherwise unchanged

on:
  workflow_dispatch:
  push:
    branches:
      - feat/chatgpt-enhancer-v0.1
    paths:
      - 'ChatGPTEnhancer/**'
      - '.github/workflows/build-enhancer.yml'

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-15
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Record workflow run id
        run: |
          printf '%s\n' "$GITHUB_RUN_ID" > .github/latest-enhancer-run-id
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .github/latest-enhancer-run-id
          git diff --cached --quiet || git commit -m "ci: record enhancer build $GITHUB_RUN_ID [skip ci]"
          git push
      - name: Show Xcode version
        run: xcodebuild -version
      - name: Build
        run: bash ./ChatGPTEnhancer/build.sh
      - name: Upload package
        uses: actions/upload-artifact@v4
        with:
          name: ChatGPTEnhancer-0.1.0-alpha50-sidebar-menu-actions
          path: ChatGPTEnhancer/build/ChatGPTEnhancer-0.1.0-alpha50-sidebar-menu-actions.zip
          if-no-files-found: error
      - name: Upload dylib
        uses: actions/upload-artifact@v4
        with:
          name: ChatGPTEnhancer-0.1.0-alpha50-sidebar-menu-actions-dylib
          path: ChatGPTEnhancer/build/ChatGPTEnhancer.dylib
          if-no-files-found: error