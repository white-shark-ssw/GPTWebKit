name: Build ChatGPTEnhancer
# alpha51 renames current Pull Latest to Sync Latest and hands a finished server result into exact-current Reload for visible page refresh
# HTTP 429 is surfaced directly instead of short automatic rate-limit retries; concurrent manual Sync taps are guarded
# current exact conversation identity and alpha50 sidebar Rename/Markdown behavior remain unchanged
# project-header presentation is paused and not modified by this candidate

on:
  workflow_dispatch:
  push:
    branches:
      - feat/chatgpt-enhancer-v0.1
    paths:
      - 'ChatGPTEnhancer/**'
      - '.github/workflows/build-enhancer.yml'

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-15
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Record workflow run id
        run: |
          printf '%s\n' "$GITHUB_RUN_ID" > .github/latest-enhancer-run-id
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .github/latest-enhancer-run-id
          git diff --cached --quiet || git commit -m "ci: record enhancer build $GITHUB_RUN_ID [skip ci]"
          git push
      - name: Show Xcode version
        run: xcodebuild -version
      - name: Build
        run: bash ./ChatGPTEnhancer/build.sh
      - name: Upload package
        uses: actions/upload-artifact@v4
        with:
          name: ChatGPTEnhancer-0.1.0-alpha51-sync-latest-rate-limit
          path: ChatGPTEnhancer/build/ChatGPTEnhancer-0.1.0-alpha51-sync-latest-rate-limit.zip
          if-no-files-found: error
      - name: Upload dylib
        uses: actions/upload-artifact@v4
        with:
          name: ChatGPTEnhancer-0.1.0-alpha51-sync-latest-rate-limit-dylib
          path: ChatGPTEnhancer/build/ChatGPTEnhancer.dylib
          if-no-files-found: error