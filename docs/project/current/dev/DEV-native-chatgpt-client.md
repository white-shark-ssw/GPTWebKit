# DEV-native-chatgpt-client

## Status

**Active**

- **Work ID**: `DEV-native-chatgpt-client`
- **Routing aliases / keywords**: `新的ChatGPT客户端 / 原生ChatGPT客户端 / 自定义ChatGPT客户端 / Native ChatGPT Client / native client`
- **Task**: 规划一个独立的 iOS 原生 ChatGPT 客户端：聊天、会话列表、输入、附件与后续扩展均使用 iOS 原生控件，不再以 ChatGPT Web 页面 / WKWebView 作为聊天 UI；网络层复用并适配真实 ChatGPT 后端通信语义。
- **User intent / acceptance criteria**: 当前阶段只做规划，不写产品代码。核心目标是从根本上避开 WebView/React/DOM 在超长会话中的加载、滚动、输入卡顿，把会话渲染与交互交给原生 iOS 列表/文本/输入控件；同时尽量保留 ChatGPT 产品层能力（账号会话、历史、项目、模型、附件、流式回复等）。任何具体私有接口、认证或流式协议必须以后通过真实请求证据确认，禁止猜测。
- **Baseline**: planning/governance baseline `main` at `92e26ae5bd0dc6ee75cc7ea3aae628ee42d5c215`. Product implementation baseline intentionally **Not selected**; this is a new standalone native-client track, not the current `ChatGPTEnhancer` product branch and not a continuation of the legacy WebView client.
- **Working branch / PR / head commit**: `feat/native-chatgpt-client` created from `main` commit `92e26ae5bd0dc6ee75cc7ea3aae628ee42d5c215`; PR not created; no product-code commits.
- **Candidate identity**: `Not allocated` — planning only, no runnable artifact requested.
- **Evidence**: repository profile confirms legacy Swift/native app history and current enhancer's native Foundation networking architecture. Existing enhancer evidence proves native code can observe/reuse real ChatGPT request context inside the official app, but a standalone client's authentication/session bootstrap and full send/stream protocol remain separate work and must be independently verified. User reports repeated WebView long-conversation optimization attempts still retain unacceptable lag and wants to abandon Web rendering for the new client.
- **Files / modules in scope**: current phase only `docs/project/current/dev/DEV-native-chatgpt-client.md` and architecture planning. Future product source root/framework are not yet selected. Existing `ChatGPTEnhancer/Sources/*` and legacy WebView code are evidence/reference only unless a later explicit decision selects reuse.
- **State owner / shared dependencies**: standalone client will need its own single authoritative session/auth owner, transport owner, conversation-tree/store owner and native UI owner; exact module names remain `Unknown / Unverified` until implementation planning is finalized. It must not borrow `CEConversationContext` or enhancer-only in-process state as a second authority.
- **Frozen / do-not-touch**: do not modify `DEV-conversation-recognition`, `DEV-conversation-usage`, their branches/candidates, or current enhancer shared-core/UI/network ownership for this planning task. Do not revive WebView DOM virtualization as the primary chat-rendering architecture.
- **Parallel conflicts checked against**: `DEV-conversation-recognition` (`feat/conversation-recognition`, alpha42) and `DEV-conversation-usage` (`feat/conversation-usage`, alpha43) both target injected `ChatGPTEnhancer`; this new task is a standalone native-client planning line on unique branch `feat/native-chatgpt-client`, with no candidate identity and no current source-file overlap. No direct parallel collision identified.
- **Completed**: task identity created; separate branch reserved; architecture direction confirmed as native iOS presentation + native networking/protocol adapter rather than WebView chat rendering. Product code remains untouched.
- **Validation state**: Planning / architecture discussion only. No code written, no checks, no CI, no artifact, no runtime test.
- **Pending**: define authentication/session bootstrap; define native transport and streaming strategy from real protocol evidence; define conversation-tree normalization and long-conversation rendering policy; define native attachment/upload path; decide UIKit vs SwiftUI boundaries; define which ChatGPT product capabilities are MVP vs later.
- **Next exact action**: continue architecture planning only. First resolve the standalone authentication/session model and the boundary between a purely native runtime and any optional login-only browser bootstrap; then map the minimum backend capabilities needed for an MVP without implementing them.
- **Rejected / do-not-repeat**: no WebView/React/DOM chat UI as the primary client; no more DOM virtualization/timer/observer tuning as the main long-conversation solution; no speculative private API paths or hard-coded undocumented auth details without real request evidence; no product code during the current planning-only stage.
- **Open questions / risks**: private ChatGPT backend/auth/protocols can change; standalone login/session persistence is likely the hardest boundary; send/stream/regenerate/edit/upload/model/project request contracts are not yet verified for the new client; public OpenAI API is not assumed to be a substitute for ChatGPT product state/history; exact native UI framework and local-cache policy remain undecided.
